# Local Redis for the Dapr state store.
# Start with: docker compose up -d
services:
  redis:
    image: redis:7-alpine
    container_name: js-state-quickstart-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]
